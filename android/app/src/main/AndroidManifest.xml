<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Runtime permissions (Phase 1). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Quick-actions persistent notification: midnight rollover + reboot
         re-show. RECEIVE_BOOT_COMPLETED is also required by
         android_alarm_manager_plus for `rescheduleOnReboot`. Inexact alarm
         is fine here — midnight rollover tolerates ~15min slop, and exact
         alarm permissions would violate Play policy for a finance app. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Persistent quick-actions notification runs as a foreground service so
         it cannot be swiped away on Android 14+ (ongoing flag alone is no
         longer sticky there). dataSync type chosen because the notification
         reflects today's synced expense total. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:label="loit"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Disable Flutter's built-in deep linking: it sets the
                 incoming intent VIEW data URI as the Navigator initial
                 route, which makes go_router try to resolve raw URIs
                 like `loit://transactions/add` as paths and throw
                 "no routes for location". The app_links plugin still
                 reads the intent and forwards URIs through
                 DeepLinkService → QuickActionsDeepLinkBus. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom-scheme fallback for room invites on devices where
                 App Links verification is unavailable. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="id.activid.loit" android:host="invite" />
            </intent-filter>

            <!-- App Links: https://loit.app/invite/* (Phase 2 room invites) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="loit.app" android:pathPrefix="/invite/" />
            </intent-filter>

            <!-- Quick-actions notification deep links: loit://scan,
                 loit://transactions[/add], loit://rooms. Handled by
                 DeepLinkService via app_links → appRouterProvider. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="loit" android:host="scan" />
                <data android:scheme="loit" android:host="transactions" />
                <data android:scheme="loit" android:host="rooms" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- PostHog v5 native init. Resolved from Gradle manifest placeholder
             (see android/app/build.gradle.kts defaultConfig.manifestPlaceholders). -->
        <meta-data
            android:name="com.posthog.posthog.API_KEY"
            android:value="${POSTHOG_API_KEY}" />
        <meta-data
            android:name="com.posthog.posthog.POSTHOG_HOST"
            android:value="https://us.i.posthog.com" />
        <!-- Temporary: enables native PostHog debug logs so `adb logcat -s PostHog`
             shows init + capture activity. Remove or set to false for release. -->
        <meta-data
            android:name="com.posthog.posthog.DEBUG"
            android:value="true" />

        <!-- Persistent quick-actions notification foreground service. Started
             from Dart via loit/quick_actions_fgs MethodChannel. -->
        <service
            android:name=".QuickActionsForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Re-posts the persistent notification when the user swipes it away
             (Android 13+ allows dismissal of FGS notifications). -->
        <receiver
            android:name=".QuickActionsRestartReceiver"
            android:exported="false" />

        <!-- android_alarm_manager_plus: midnight rollover for quick-actions
             persistent notification. Plugin's own manifest entries don't
             always merge cleanly under our package id, so declare them
             explicitly. RebootBroadcastReceiver starts disabled; the plugin
             enables it via setComponentEnabledSetting when an alarm is
             scheduled with rescheduleOnReboot=true. -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
